profiles {
    standard {
        process.executor = 'local'
        process.containerEngine = 'docker'
        process.cpus = 2
        process.memory = '2 GB'
        workDir = './work'
        publishDir = './results'
    }
}
