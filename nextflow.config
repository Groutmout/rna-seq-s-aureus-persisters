docker.enabled = true

process {
  executor = 'local'
  container = 'ubuntu:22.04'       // conteneur par défaut (pour 'hello', etc.)

  withName: bowtie_build {
    container = 'bouty:latest'     // ton image locale Bowtie 0.12.7
  }
}

